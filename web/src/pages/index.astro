---
import Layout from '~/layouts/layout.astro'
import { getLiveCollection } from 'astro:content'
import { Debug } from 'astro:components'

const posts = await getLiveCollection('posts', {})
---

<Layout>
  <h1>Hello World!</h1>
  <Debug {posts} server:defer />
</Layout>
